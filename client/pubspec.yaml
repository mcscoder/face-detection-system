name: face_detection_client
description: Flutter client shell for the local face detection system.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  camera: ^0.12.0+1
  flutter:
    sdk: flutter
  image_picker: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - env/mobile.json
